## 要做的事
- 在仓库根目录的 `release/` 下新增 `release_v0.1.10.md`，包含本次改动的亮点、配置示例与迁移指南。

## 内容结构
- 概览：版本号、日期、变更摘要（核心默认 ObjectMapper、多数据源、Feign 动态客户端、示例更新）。
- 核心模块（core）：
  - 默认 `ObjectMapper`（snake_case + JavaTime）
  - 通过 `pgrest.datasources` 支持多数据源并注册命名 `PgRestClient` Bean
  - 配置 YML 示例与注入示例
- Feign 模块：
  - 新增 `pgrest.feign.clients` 动态注册 FeignClient（按 `serviceName`，支持 `gateway|direct`、`url`、`pathPrefix`、`decode404`）
  - Bean 命名与使用示例
- 示例工程：
  - 新增 `spring-boot-core-multids`（多数据源）
  - 在 `spring-boot-alicloud-crud` 中增加动态 Feign 用法（启动即运行示例）
- 兼容性与注意事项：与 Starter/现有 Feign 的关系，`ObjectMapper` 冲突避免。
- 迁移指南：如何从 0.1.9 升级到 0.1.10（添加配置即可，无破坏性变更）。

## 不做的事
- 不修改 POM 版本号与构建配置，仅新增发布说明文档。