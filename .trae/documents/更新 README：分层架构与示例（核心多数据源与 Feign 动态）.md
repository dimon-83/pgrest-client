## 目标
- 在根目录 `README.md` 增加“按层级划分”说明，并结合示例展示：
  1) core：默认 ObjectMapper 与多数据源配置、命名 Bean 注入
  2) starter：单一数据源与网关控制器
  3) feign：基于 `service-name` 的动态 FeignClient 注册与使用

## 内容结构
- 按层级划分（三层）
- 核心（新增能力与 YAML/注入示例）
- Starter（YAML 与用法简述）
- Feign（动态客户端属性、YAML 与注入示例）
- 示例工程引用（文件路径与快速运行提示）

## 注意
- 保持中文 README，不改动英文版文件；不调整依赖版本号；仅更新文档说明与示例代码块。